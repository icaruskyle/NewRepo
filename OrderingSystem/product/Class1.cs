namespace product
{
    public class Product
    {
       <List>Products 
            
            static void AddProduct(string, double)
        {
            Product prod = new Product();
            prod.name = n;
            prod.name = p;
            products.Add(prod);
        }
        AddProduct (Product prod)
        {
            products.Add(prod);
        }
        static DisplayMenu (string tite)
        {
            foreach () ;
        }
    }
}
